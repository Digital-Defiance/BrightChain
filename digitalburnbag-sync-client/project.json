{
  "name": "digitalburnbag-sync-client",
  "sourceRoot": "digitalburnbag-sync-client/src",
  "projectType": "library",
  "tags": ["type:lib", "scope:desktop"],
  "implicitDependencies": ["brightchain-lib", "digitalburnbag-lib"],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/digitalburnbag-sync-client",
        "main": "digitalburnbag-sync-client/src/index.ts",
        "tsConfig": "digitalburnbag-sync-client/tsconfig.lib.json",
        "assets": [
          {
            "input": "digitalburnbag-sync-client",
            "glob": "*.md",
            "output": "."
          },
          {
            "input": "digitalburnbag-sync-client",
            "glob": "package.json",
            "output": "."
          }
        ]
      },
      "dependsOn": ["^build"],
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "digitalburnbag-sync-client/jest.config.cts",
        "passWithNoTests": true
      },
      "cache": true
    },
    "publish": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm publish --access public",
        "cwd": "dist/digitalburnbag-sync-client"
      }
    }
  }
}
