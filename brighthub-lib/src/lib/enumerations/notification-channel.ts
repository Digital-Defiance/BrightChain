/**
 * Delivery channels for notifications
 */
export enum NotificationChannel {
  /** In-app notifications */
  InApp = 'in_app',
  /** Email notifications */
  Email = 'email',
  /** Push notifications */
  Push = 'push',
}
