/**
 * Waitlist Management — barrel export
 *
 * Re-exports all waitlist types so consumers can import from
 * `scheduling/waitlist`.
 *
 * @module scheduling/waitlist
 */

export * from './waitlistTypes';
