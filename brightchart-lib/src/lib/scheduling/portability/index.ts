/**
 * Scheduling Portability — barrel export
 *
 * Re-exports all portability types so consumers can import from
 * `scheduling/portability`.
 *
 * @see Requirements 16.1, 16.2
 * @module scheduling/portability
 */

export * from './schedulingPortability';
