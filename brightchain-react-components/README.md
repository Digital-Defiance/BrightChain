# brightchain-react-components

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build brightchain-react-components` to build the library.

## Running unit tests

Run `nx test brightchain-react-components` to execute the unit tests via [Jest](https://jestjs.io).
