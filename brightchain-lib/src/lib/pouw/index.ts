/**
 * @fileoverview Barrel export for the PoUW (Proof of Useful Work) client module.
 *
 * Exports the browser-safe `computeWorkUnit()` function and the
 * `ProgressCallback` type for use by frontend consumers.
 */

export { computeWorkUnit, type ProgressCallback } from './computeWorkUnit';
