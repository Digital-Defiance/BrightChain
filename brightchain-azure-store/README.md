# brightchain-azure-store

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build brightchain-azure-store` to build the library.

## Running unit tests

Run `nx test brightchain-azure-store` to execute the unit tests via [Jest](https://jestjs.io).
