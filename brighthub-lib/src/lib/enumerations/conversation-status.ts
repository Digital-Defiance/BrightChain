/**
 * Status of a conversation for a user
 */
export enum ConversationStatus {
  /** Active conversation */
  Active = 'active',
  /** Archived conversation */
  Archived = 'archived',
  /** Muted conversation */
  Muted = 'muted',
}
