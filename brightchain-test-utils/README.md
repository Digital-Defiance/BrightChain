# brightchain-test-utils

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build brightchain-test-utils` to build the library.

## Running unit tests

Run `nx test brightchain-test-utils` to execute the unit tests via [Jest](https://jestjs.io).
