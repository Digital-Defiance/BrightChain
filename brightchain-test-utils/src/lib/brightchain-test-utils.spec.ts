import { brightchainTestUtils } from './brightchain-test-utils';

describe('brightchainTestUtils', () => {
  it('should work', () => {
    expect(brightchainTestUtils()).toEqual('brightchain-test-utils');
  });
});
