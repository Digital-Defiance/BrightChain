/**
 * Categories for grouping notifications
 */
export enum NotificationCategory {
  /** Social interactions (likes, replies, mentions, reposts, quotes) */
  Social = 'social',
  /** Direct messages and message requests */
  Messages = 'messages',
  /** Follow-related notifications */
  Connections = 'connections',
  /** System alerts and announcements */
  System = 'system',
}
