/**
 * Reminders sub-module — barrel export
 *
 * Re-exports all reminder types so consumers can import from
 * `scheduling/reminders`.
 *
 * @module scheduling/reminders
 */

export * from './reminderTypes';
